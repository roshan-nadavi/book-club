import ChangePasswordForm from "@/components/auth/ChangePasswordForm";

export const metadata = { title: "Change password — Book Club" };

export default function ChangePasswordPage() {
  return <ChangePasswordForm />;
}